using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace DPO.Web.Helpers
{
    public static partial class Extensions
    {
        //public static MvcHtmlString DropDownProjectDarComStatusTypeListFor<TModel, TProperty>(this HtmlHelper<TModel> htmlHelper, Expression<Func<TModel, TProperty>> expression, DropDownModel model)
        //{
        //    return DropDownProjectDarComTypeListFor(htmlHelper, expression, model, null);
        //}

        //public static MvcHtmlString DropDownProjectDarComStatusTypeListFor<TModel, TProperty>(this HtmlHelper<TModel> htmlHelper, Expression<Func<TModel, TProperty>> expression, DropDownModel model, object htmlAttributes)
        //{
        //    TProperty propVal = expression.Compile()(htmlHelper.ViewData.Model);

        //    return htmlHelper.DropDownListFor(expression, model, "Choose....", "No project Dar/Com found", htmlAttributes, true);
        //}
    }
}