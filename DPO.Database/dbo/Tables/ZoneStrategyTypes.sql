CREATE TABLE [dbo].[ZoneStrategyTypes] (
    [ZoneStrategyTypeId] TINYINT           NOT NULL,
    [Description]     NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_ZoneStrategyType] PRIMARY KEY CLUSTERED ([ZoneStrategyTypeId] ASC)
);

